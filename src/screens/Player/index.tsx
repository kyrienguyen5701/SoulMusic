import React from "react";
import {TouchableOpacity, View} from "react-native";

const Player = () => {

    return (
        <View style={{
            backgroundColor: 'red',
            height: '100%',
        }}>
            <View style={{
                marginHorizontal: 16,
            }}>
                <View style={{
                    display: 'flex',
                    flexDirection: 'row'
                }}>
                    <TouchableOpacity>
                        <
                    </TouchableOpacity>
                </View>
            </View>
        </View>

    );
}

export default Player;
